// PoE2 orbs — empty until the user provides specs (cost, preconditions,
// outcome pools, weights, ilvl gating). Kept as a typed list so the engine
// can iterate without per-game special cases.

/** @type {import('../../engine/types.js').Action[]} */
export const actions = [];
